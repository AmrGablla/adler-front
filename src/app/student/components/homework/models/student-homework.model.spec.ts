import { StudentHomeworkById } from './student-homework-byId.model';

describe('StudenHomework', () => {
  it('should create an instance', () => {
    expect(new StudentHomeworkById()).toBeTruthy();
  });
});
